 # Use WebAssembly version of Rollup for cross-platform compatibility
prefer-install=@rollup/wasm-node=true

# Turn off optional dependencies that cause issues
optional=false

# Use exact versions to reduce dependency issues  
save-exact=true

# Increase network timeout for better reliability on CI
network-timeout=100000